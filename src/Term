클래스 : 객체와 메서드를 설계하는 곳
인스턴스 : 클래스로부터 만들어지는 각각의 객체를 그 클래스의 "인스턴스"라고 한다.

접근제한
public > protected > default > private

public : 접근 제한이 없다.
protected : 동일한 패키지 및 하위 클래스(패키지가 달라도 하위 클래스)에서 접근 가능
default : 동일한 패키지내에서만 접그 가능
private : 같은 클래스 내에서만 접근 가능

Overloading 과 Overriding

Overloading(메소드 중복정의) :메소드 기능은 같고 사용되는 매계변수(자료형)가 다른경우 동일한 이름의 메소드 재정의 하는것
Overriding(메소드 재정의) : 상위 클래스를 상속 받은 하위 클래스에서 상위 클래스에 정의된 메소드를 재정의 하는것
